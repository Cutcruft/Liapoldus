/** Жизненно-важные константы приложения (слайс 2.9). */

/** Версия приложения, показывается в футере и на странице настроек. */
export const APP_VERSION = '0.1.0 (MVP)';

/** Переключатель dev-режима (включение hot-reload/dev-индексов). */
export const ENV_DEV = 'development';

/** Путь dev-канала (клиентский сервер, слайс 6). */
export const DEFAULT_DEV_WS_PATH = '/dev/build/ws';

/** Путь канала событий сборок (admin, M3). */
export const DEFAULT_BUILDS_WS_PATH = '/api/builds/ws';

/** Дебаунс автосохранения схемы в редакторе (мс). */
export const AUTOSAVE_DEBOUNCE_MS = 1500;