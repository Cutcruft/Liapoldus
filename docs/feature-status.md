# Статус функций

`README.md` описывает целевую архитектуру Liapoldus. Этот файл показывает, что уже реализовано в текущем первом срезе.

## Реализовано

| Область | Статус | Что проверяется |
| --- | --- | --- |
| Go backend | Готово | Запуск двух HTTP-серверов (admin, client) из переменных окружения |
| Admin REST API | Готово | API из `docs/api-admin.md`: sites/pages/content/assets/routes/forms/snapshots |
| Client REST API | Готово | API из `docs/api-client.md`: content (merge по locale), assets, forms, `/runtime/*` |
| Edge routing | Готово | serve asset, redirect (включая группы `$1..$9` и keepQuery), render page → 404 |
| Site | Реализовано | Создание, чтение, список, обновление, удаление, `defaultLocale`, `hosts`, резолв по Host |
| Page | Реализовано | Создание, чтение, список, update tree, версии страницы |
| Component tree | Реализовано | `Container`, `Text`, `Image`, `Button`, props и children |
| Component validation | Реализовано | ID, тип компонента, глубина дерева |
| ComponentDefinition | Реализовано | Реестр по сайту: id, schema (JSON Schema), metadata; дерево ссылается на `definitionId` и валидируется жёстко по schema (R4) |
| Component versioning (git) | Реализовано | Bare-репо на сайт (go-git, `internal/infra/git`); `Release` = коммит (`src/definition.tsx` + `schema.json` + `metadata.json`); история версий, checkout по sha, rollback |
| Page versioning | Реализовано | Новая версия при обновлении дерева |
| Snapshot | Реализовано | Фиксация актуальной версии каждой страницы, чтение, версии, удаление |
| Content | Реализовано | Поля + переводы по locale, merge на стороне клиента, batch, коллекция `strings` |
| Asset storage | Реализовано | Загрузка (multipart), метаданные с variants, выдача байтов с ETag, удаление |
| Routes | Реализовано | matcher (regex) + priority, действия serve-asset/redirect/render-page, валидация |
| Forms | Реализовано | Определение, серверная валидация (required/minLength/email), сабмиты |
| Auth (admin) | Реализовано | Bearer token через `LIAPOLDUS_ADMIN_TOKEN` (пустой = открыто) |
| Runtime health | Готово | `GET /healthz` на admin и client |
| Persistence | Реализовано | PostgreSQL adapter (миграции `001`–`004`) и in-memory adapter |
| Build (synchronous, Этот 3) | Реализовано | `POST /api/sites/{id}/builds` {snapshotId, environment} → синхронная сборка (queued→ready/failed), no-op повторной публикации; материализация (workspace: `src/entry.tsx` + `src/definitions/*.tsx` + `manifest.json`), esbuild как библиотека Go, артефакты `build/<site>/<env>/<snapshot>/`; статика отдаётся публично на `/build/...` |
| Dev-пересборщик (Этап 3 §7) | Реализовано | `internal/infra/build/rebuilder`: fsnotify → esbuild `Context` (Incremental) → publish + бродкаст `DevRebuildEvent`; `Hub` с relay `Current`; WS-канал `GET /dev/build/ws` (фильтр `?siteId=`, failed не заменяет последний удачный артефакт); привязка к boot-рантайму — Этап 5 |
| Shared-бандлы (Этап 3 §8) | Реализовано | Реальные ESM-бандлы (react/react-dom/jsx-runtime 18.3.1 + ui-runtime 0.1.0) из `scripts/build-shared`, go:embed → `/build/_shared/<key>/<version>.js`; манифест содержит import-map `shared`; `Externals` += `react/jsx-runtime`. CDN fetch+lock — R9 |
| Runtime-контракт (Этап 4) | Частично | **Boot-контракт реализован**: `GET /runtime/contract` (снапшот-пиннинг через `versionId` | последний ready-билд окружения) на `application/runtime.Service`; JSON совпадает с `parseDescriptors`/`extractTree` ui-runtime; автономный boot-скрипт (`tests/integration/fixtures/ui-runtime-core.mjs` + `boot.mjs`, реальный `boot()` в integration-тесте). Осталось: `/runtime/tree`, `/runtime/routes`, `/runtime/tokens` + API компонент-реестра (`ComponentRegistry` для entry-шаблона — сегодня только `RuntimeRegistry`) |
| Сторонние npm-пакеты | Фаза 1 + Фаза 2 (слайсы 1–5) | `deps.Service` (CRUD + `ResolveLock` **вложенный версионный graph**: BFS граней, reuse-first-satisfying, `LockedDep{Hoisted,RequestedBy}`, exact+sha512), registry-фетчер, диск-стор tarball-блобов (`<data>/deps`), материализация (hoisted → `node_modules/<name>`, несовместимая версия → `node_modules/<parent>/node_modules/<name>`) + esbuild per-dep бандлы `dist/_deps/<name>@<version>.js` (scoped `%2F`), subpath-импорты site-кода **и внутри вершинных deps** → `dist/_deps/<name>@<version>/<subpath>.js` + import-map/externals, `manifest.deps`/`manifest.externals`, **CSS/не-JS ассеты**: combined css соседним `.css`-артефактом (esbuild css-loader), bare css-subpath → свой `.css` + JS-stub в import-map, `manifest.styles` для `<link>`, non-CSS ассеты → fail с hint, `DepBuildError` для несовместимых, **peer-политика (peer-fail)**: registry парсит `peerDependencies`/`peerDependenciesMeta`, post-pass `checkPeers` — обязательный peer удовлетворяется экземпляром графа или фиксированным shared external (react 18.3.1 и др.), иначе `ErrUnsatisfiedPeer` (422); `LockedDep` фиксирует peer-метаданные (SBOM), legacy flat-locks раскладываются как прежде, e2e (httptest-registry + реальный esbuild: nested-версии, legacy-lock, css-артефакты, peer-политика). Осталось (Фаза 2/3): allowlist scopes, кэш-лимиты/эвикция, `cmd/dependency-build`. Спека `docs/backend/dependency-service.md` |
| Admin SPA (Этап 6, M0) | Реализовано | Монорепо (`ui-runtime`/`ui-kit`/`admin`, root workspaces). Спека `docs/editor-spec.md`. ui-runtime: публичные экспорты + обобщённый slice-store (zustand-обёртка `createSliceStore`/`useSelector`, админка zustand не импортирует). `@liapoldus/ui-kit`: layout-примитивы Tailwind v4 (Box/Stack/Inline/Columns/Grid/Spacer/Divider/Frame/Sidebar/SplitPane) + 25 тестов. `admin`: Vite+React+TS+Tailwind v4+React Router, proxy `/api`→:8080 и `/runtime|/build|/dev`(ws)→:18080, AppShell+admin-context+роуты-заглушки. admin-runtime: AdminApi (fetch, Bearer-токен, норм. ошибки 400/401/4xx/transport), реестр операций `OPERATIONS` + `runOperation`, token-store на slice-store, i18n строки; 20 тестов на mock-fetch. M1-слайс «Список+CRUD»: ops listPages/deletePage, `useOperation`, ConfirmButton (без window.confirm), Field/EntityTable, страницы Sites/SiteHome/SitePages/SiteRoutes + роуты + EditorPlaceholder; 13 тестов страниц (админка 33/33, корень 285/285) |
| Admin SPA (Этап 6, M1-слайс 2: ядро редактора) | Реализовано | Редактор дерева: `tree-utils` (immune-утилиты, uid), `page-store` на ui-runtime slice-store с undo/redo (past/future, HISTORY_CAP) и dirty через savedKey; TreePanel (add-меню из builtin-компонентов, ↑/↓/✕). Инспектор: `schemas.ts` BUILTIN_COMPONENTS (Container/Text/Image/Button) + builtinFieldSchema; `SchemaForm` — генератор полей из JSON-Schema (текст/число/checkbox/enum), live-валидация (required/type/enum/range). Bindings: `BindingSource` (`{source,path}`), picker literal/content/route/query/operation/form. Draft+автосейв: EditorPage (debounce 1500 → `saveTree`), SaveIndicator («Сохранено, vN»/«Есть несохранённые изменения»/ошибка). Canvas-превью: design-mode рендер (Container/Text/Image/Button + `{{source:path}}`). Маршрут `/sites/:siteId/pages/:pageId`. Tests: tree-utils 8, page-store 11, SchemaForm 5, EditorPage 6 (load/autosave-PUT/undo-redo/binding/delete/error). Админка 66/66, корень 318/318 |
| Admin SPA (Этап 6, M1-слайс 3: canvas-превью iframe) | Реализовано | Таб «Дизайн | Превью»: CanvasPreview (iframe `/build/{site}/{development}/{snapshot}/dist/index.html` + локальный design-mode рендер по умолчанию). Авто-сборка dev build после автосейва: `createSnapshot` «Dev preview» → `createBuild` development → poll getBuild → url, ротация превью-снапшота (DELETE прошлого). `preview-store` на slice-store: coalescing/queued, персист последнего снапшота в `localStorage liapoldus.preview.{site}`. WS `/dev/build/ws?siteId=` (`dev-ws.ts`, reconnect/backoff, инжект фабрики для тестов) → `DevRebuildEvent` reload. ops `createBuild`/`getBuild`/`deleteSnapshot`. Tests (Слайс 3): preview-store 8, dev-ws 4, CanvasPreview 3, ops 3, EditorPage автосборка+iframe 1. Админка 85/85, корень 337/337 |
| Admin SPA (Этап 6, M2-слайс 1: контент) | Реализовано | Контент по коллекциям: ops createContent/deleteContent/deleteTranslation (дополнили listContents/getContent/updateContent/putTranslation). `JsonFieldsEditor` — редактор `Record<string,unknown>` строками «ключ/тип/значение» (string/number/bool/json textarea): инлайн-валидация, NaN/битый JSON не попадают в объект. SiteContentsPage (`/sites/:siteId/contents`): таблица items, фильтр-чипы по коллекциям, форма создания `{collectionId, id?, fields}`, удаление через ConfirmButton. ContentEditorPage (`/sites/:siteId/contents/:contentId`): base = `defaultLocale` (из getSite) + переводы overlay — табы локлей, «Добавить локаль», PUT updateContent/putTranslation, DELETE deleteTranslation. Навигация SiteHomePage + роуты. Tests: ops 4, JsonFieldsEditor 6, SiteContentsPage 4, ContentEditorPage 4. Админка 103/103, корень 355/355 |
| Admin SPA (Этап 6, M2-слайс 2: ассеты) | Реализовано | Библиотека ассетов: ops getAsset/deleteAsset (uploadAsset уже был — multipart FormData: `api.request` сам снимает `Content-Type` и не JSON.stringify). `assets/`: AssetThumb (img `/api/assets/{id}/file` для image/*, иначе бейдж расширения), assetUrl/formatBytes. SiteAssetsPage (`/sites/:siteId/assets`, был Placeholder): таблица превью/имя/mime/размер, форма upload (file + опц. name), открытие файла, удаление через ConfirmButton. AssetPicker — модальная библиотека: поиск, inline-upload, выбор id (клик закрывает). Инспектор: format `asset` в JSONSchemaProperty (Image.assetId), SchemaForm рендерит AssetFieldControl (trigger + миниатюра по getAsset + сброс). Design-превью Image: src → `/api/assets/{id}/file`. Tests: ops 3, SiteAssetsPage 4, AssetPicker 3, EditorPage +1 (asset-picker flow). Админка 114/114, корень 366/366 |
| Admin SPA (Этап 6, U0+U1: shadcn/ui) | Реализовано | shadcn/ui (new-york, neutral) в admin: тема `@theme inline` oklch в styles.css (+`.dark`-блок, tw-animate-css), `components.json`, `src/lib/utils.ts` (`cn`), алиас `@/` (tsconfig+vite). Базовый набор в `src/components/ui/`: button/input/label/badge/skeleton/alert-dialog/sonner (+lucide-react, radix, cva, clsx, tailwind-merge — React 18 peer-чисто). `src/test-setup.ts` — jsdom-полифилы Radix (ResizeObserver/HasPointerCapture/scrollIntoView/matchMedia). `ConfirmButton` → AlertDialog (API сохранён, тесты confirm'ов не правились). `SitesPage` — эталон на Button/Input/Skeleton. `Toaster` в main.tsx. Админка 114/114, корень 366/366, build JS 389 kB / CSS 31.79 kB |
| Admin SPA (Этап 6, M2-слайс 3: формы) | Реализовано | Формы: ops getForm/updateForm/deleteForm/listSubmissions (createForm/listForms уже были). `forms/form-utils`: slugifyFormId (`form.<slug>`, пробелы→доты), createDefaultDefinition (`{id, fields:[], submit:{endpoint}}`), validateDefinition (JSON + схема §10: id, fields type ∈ {text,email,password,number,select,checkbox,textarea,custom}, submit.target\|endpoint). SiteFormsPage (`/sites/:siteId/forms`, был Placeholder): список имя/id/число полей, создание name→id, удаление через ConfirmButton, «Открыть» → редактор. FormEditorPage (`/sites/:siteId/forms/:formId`): имя + монo-textarea definition с валидацией при сохранении (PUT updateForm), табы «Определение | Сабмиты», сабмиты listSubmissions (id/дата/payload `<details>`-JSON), DELETE → возврат в список. Tests: ops 4, form-utils 13, SiteFormsPage 4, FormEditorPage 5. Админка 141/141, корень 393/393, build JS 399.94 kB / CSS 32.30 kB |
| Admin SPA (Этап 6, M2-слайс 4: роуты) | Реализовано | Роуты: ops getRoute. `routes/route-utils`: isValidRegex (try/new RegExp), validateRouteFields (matcher непустой+валидный regex, target непустой, redirect.status ∈ {301,302,307,308}), normalizeAction (redirect + status/keepQuery; serveAsset/renderPage ← цель), overlapWarnings (клиентская эвристика пересечений: точный дубль → duplicate; префикс со снятыми якорями ^/$ и длиной ≥ 3 → prefix). SiteRoutesPage: форма создания с live-валидацией на blur и блокировкой submit, redirect = select статуса (301/302/307/308) + чекбокс keepQuery, амбер-предупреждения пересечений (не блокируют), в списке у redirect показывается status/keepQuery, колонка «Редактировать». RouteEditorPage (`/sites/:siteId/routes/:routeId`): getRoute → форма с валидацией, PUT updateRoute `{matcher, priority: Number, action}`, предупреждения пересечений с excludeId, DELETE → возврат в список. Tests: route-utils 19, SiteRoutesPage 6, RouteEditorPage 3, ops +2. Админка 168/168, корень 420/420, build JS 408.20 kB / CSS 32.73 kB |

## Пока не реализовано

- Binding и runtime data sources (props ← контент/route/query/операция);
- Theme;
- Operation, Provider и frontend SDK;
- прямые HTTP/GraphQL-интеграции из frontend;
- server-only operations;
- ESB extension layer (registry, metadata routing, `Call`/`Stream`);
- Plugin management и декларации зависимостей (fetch + lock с CDN/registry);
- версии всех объектов кроме Page;
- полноценный Snapshot со всеми типами объектов;
- shared-бандлы (`build/_shared`) с fetch+lock (R9);
- Development/Production environments (client работает с текущими данными напрямую);
- runtime publication и rollback;
- Redis/cache policies;
- production lifecycle и динамическая загрузка extension registry.

## Спроектировано (спек, реализация не начата)

| Область | Документация |
| --- | --- |
| ui-runtime: архитектура и слои (контент+локализация, ассеты, единый роутинг/edge) | `docs/ui-runtime/spec.md` |
| ui-runtime: JSON-дескрипторы (контракт для backend, вкл. поллинг/content-locale/ассеты/роуты) | `docs/ui-runtime/json-descriptors.md` |
| ui-runtime: полный спек тестов (контракт для реализации, вкл. asset/edge-тесты) | `docs/ui-runtime/test-spec.md` |
| Backend REST API (разделение admin/client, content с `?locale` и translations, ассеты, формы, runtime-роутинг) | `docs/api.md`, `docs/api-admin.md`, `docs/api-client.md` |

Наличие пункта в основном README не означает, что он уже доступен через API. Перед реализацией каждого следующего блока его нужно добавить в этот статус и покрыть тестами.